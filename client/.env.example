# Базовый адрес API. По умолчанию /api — запросы идут через прокси:
# в разработке его держит Vite, в контейнере — nginx.
# Значение по умолчанию рабочее, файл переменных опционален.
VITE_API_URL=/api

# Порт dev-сервера Vite. В Docker Compose клиент отдаётся nginx на 5173.
VITE_PORT=5173
